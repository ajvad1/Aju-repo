# Handwritten Digit Recognition

This project uses machine learning to recognize handwritten digits using the MNIST dataset.

## Tech Used
- Python
- TensorFlow / Keras
- NumPy
- Matplotlib

## How to Run
1. Install dependencies:
   pip install -r requirements.txt

2. Run the program:
   python main.py

## Output
The model predicts handwritten digits from input images.
